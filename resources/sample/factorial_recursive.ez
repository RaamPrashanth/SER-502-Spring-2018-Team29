/# Program to compute Factorial using recursion #/
variable x;
write "X";
read x;

x = factorial(x);
write "Factorial";
write x;

function factorial(x) {

	if (x > 1) then {

    variable y;

    y = x - 1;

    x = x * factorial(y);

    } 

    return x;

}