/# Program to compute count backwards series using recursion #/
variable n;
write "N";
read n;	
countBackwards(n);
function countBackwards(n)
{
if(n>0)
then 
{ 
variable a;
a=n-1;
write "Count";
write n;
countBackwards(a);
}
}